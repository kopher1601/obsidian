일반적으로 시스템은 암호를 일반 텍스트로 관리하지 않고 공격자가 암호를 읽고 훔치기 어렵게 하기 위한 일종의 변환 과정을 거친다. 스프링 시큐리티에는 이 책임을 위해 정의된 별도의 계약이 있다.

- `PBKDF2` : 반복 횟수 인수만큼 HMAC 를 수행하는 아주 단순하고 느린 해싱 함수이다.
- `BCrypt` : 강략한 해싱 함수인 bcrypt 를 사용
- `Delegating` : 같은 계약의 다른 구현 인스턴스에 작업을 위임한다
![[Screenshot 2025-10-29 at 9.01.13.png]]


