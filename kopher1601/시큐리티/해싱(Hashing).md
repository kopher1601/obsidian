해싱(Hashing) 은 함수가 한 방향으로만 작동하는 특정한 유형의 인코딩이다. 따라서 해싱 함수의 출력 `y` 에서 입력 `x` 를 얻을 수 없다. 그러나 출력 `y` 가 입력 `x` 에 해당하는지 확인할 수 있는 방법이 반드시 있어야 하므로 해시는 인코딩과 일치를 위한 한 쌍의 함수로 볼 수 있다. 
해싱 함수가 `x -> y` 라면 일치 함수 `(x,y) -> boolean` 도 있다.
